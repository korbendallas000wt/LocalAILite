# Журнал разработки LocalAILite

Релиз 1.8.0 — 2026-08-31
Цель: первый релиз с модулем загрузки моделей + критические фиксы.
Что вошло:
- Счётчик контекста (прогрессбар токенов в нижней панели, обновляется при смене модели/промпта/сообщений)
- Вложения в чат с карточкой (📄 имя · размер · ✕, клик открывает файл, при отправке впечены в запрос)
- Менеджер моделей (меню «Модели» → «Менеджер моделей...», список доступных, фильтр по железу, вердикты по ОЗУ, скачивание с прогрессом и отменой)
- FolderDialog (обёртка над QFileDialog с режимами navigate/select)
- Фикс краша QThread при закрытии настроек (updater.py v2.1: проверка версий через QNetworkAccessManager вместо блокирующего urlopen)
- Фиксы скачивания: ложный успех fp16-моделей, прогрессбар на 5%, падение загрузки fp16, прыжок вкладки после скачивания
- Скриншоты в README (4 кадра)
Временно отключено перед релизом:
- Подсветка прогрессбара токенов (зелёный/оранжевый/красный) — ломала форму полосы
- Превью по шагам генерации — сильно тормозят процесс
Коммит релиза: `a24b682` в main, тег `v1.8.0`
Следующий шаг: консолидация управления моделями (влить удаление/открытие папки из `diffusers_models_dialog.py` в Менеджер, убрать дубль из настроек Diffusers).

Хотфикс README — 2026-08-31
Проблема: README.md не попал в релиз v1.8.0 из-за старого исключения в sync_repo.sh (скрипт копировал только main.py, а README жил только в Repo).
Обновлятор перезаписал новый README старым из main, а новый остался только в dev на GitHub.
Решение:
- Исправлен sync_repo.sh: README.md добавлен в список копируемых файлов первого блока (строка с main.py)
- Обновлены комментарии в скрипте (убрано упоминание "НЕ удаляет README.md")
- Запущена синхронизация заново, README скопирован в Repo
- Хотфикс закоммичен отдельно в main (коммит e6d592b), без мусорного test_downloader.py
Принцип на будущее: README.md теперь зеркалируется из рабочей директории как обычный файл, а не живёт изолированно в Repo.


## План: Модуль загрузки моделей (3 этапа)

> Статус: Этапы 1–3 выполнены (2026-08-30). Обсуждено 2026-08-29, старт Этапа 1 — вечер 2026-08-29.

**Временно отключено перед релизом:**
- Подсветка прогрессбара токенов (зелёный/оранжевый/красный) — ломала форму полосы, вернём с системными цветами
- Превью генерации по шагам — сильно тормозят процесс, вернём после подбора лёгкого декодера
> Цель: дать пользователю возможность докачивать модели после установки (инсталлер ставит стартовый набор, этот модуль — расширяет).

### Контекст и договорённости
- **Раздельные реализации, общий каркас**: механика скачивания у Ollama (`ollama pull`) и Diffusers (`huggingface_hub`/`requests`) разная, но общая логика (прогресс, отмена, проверка места, верификация) — одна.
- **Не визард**: один диалог со списком моделей, фильтром по железу и прогрессбаром. Лишних кликов не добавляем.
- **Фильтр по железу** переиспользует вердикты из `installer/advisor.py`.
- **Два реестра**: доступные модели (что можно скачать) и установленные (что на диске, уже есть в `models_registry.py`).
- **Переиспользуем** логику из `installer/steps/step_models.py`, `core/models_registry.py`, `core/model_validator.py`, `installer/advisor.py`.

### Этап 1: Фундамент — реестр доступных моделей ✅ ВЫПОЛНЕНО
Коммиты `9e36579` (feat) + `1ccda42` (fix) в dev
Архитектурное решение: дефолтный список моделей захардкожен в коде (константа `AVAILABLE_MODELS_DEFAULTS`), файл `data/shared/registry/available_models.json` используется только как override для кастомизации пользователем. Работает из коробки без файла.
Метод `list_available_models(config)` в `core/models_registry.py` — читает файл если есть, иначе возвращает дефолт.
Структура данных: `{"ollama": [...], "diffusers": [...]}`, поля модели: name, source, size_gb, min_ram_gb, tag, description

### Этап 2: Ядро — модуль скачивания ✅ ВЫПОЛНЕНО
Коммит `8fb55b6` в dev
- `core/model_downloader.py`: общий контракт + сигналы (прогресс, завершение, ошибка, отмена)
- `OllamaDownloader` — через `ollama pull` (QProcess), парсинг прогресса из stdout
- `DiffusersDownloader` — через `huggingface_hub` или `requests` (QProcess, изоляция тяжёлых зависимостей)
- Проверка свободного места перед стартом, верификация после (через `model_validator.py`)
- Тестирование из терминала без UI (скачиваем маленькую модель, видим прогресс)
- Коммит → пуш в dev

### Этап 3: UI — Менеджер моделей ✅ ВЫПОЛНЕНО
- `ui/dialogs/model_manager_dialog.py`: список моделей, фильтр по железу, вердикты по ОЗУ, скачивание с прогрессом и отменой
- Интеграция: меню или существующий `diffusers_models_dialog.py` (решим по месту)
- Тестирование в приложении на Manjaro
- Коммит → пуш в dev

### Принцип реализации
Снизу вверх: данные → логика → UI. Каждый этап — логически завершённое изменение, тестируется и пушится отдельно. По одному этапу за сессию.

---

## 2026-08-30 (сессия 3): Этап 3 — Менеджер моделей (UI) + фиксы скачивания ✅

Цель: диалог «Менеджер моделей»: просмотр доступных моделей, вердикты по железу, скачивание с прогрессом и отменой. На живых тестах вскрыт и устранён пласт багов скачивания и загрузки моделей.

Что сделано:

Данные и реестр (core/models_registry.py):
- Поле "packaging": "hf_cache" в записях секции diffusers (контракт Этапа 1).
- Новая функция list_installed_ollama_models(config) — список установленных моделей Ollama сканированием manifests/ без запущенного сервера.

Диалог (ui/dialogs/model_manager_dialog.py, новый):
- Вкладки по секциям реестра, фильтр по features/* (секция diffusers маппится на флаг sdxl).
- Список: имя, размер, мин. ОЗУ, статус, кнопка «⬇ Скачать»; внизу — панель деталей с вердиктом по ОЗУ.
- Чекбокс «Только совместимые» (по умолчанию выключен); несовместимые приглушены цветом из системной палитры (без хардкода — защита от артефактов тем в Qt6).
- Одна загрузка за раз, блокировка остальных кнопок, кнопка «Отменить», закрытие диалога отменяет активную загрузку.
- Статус «установлена» для Diffusers — сравнение по full_name (HF repo id), а не по отображаемому имени (обход чувствительности _beautify_name к регистру).

Меню (ui/main_window.py):
- Меню «Модели» между «Файл» и «Инструменты», пункт «Менеджер моделей...».

Баги, вскрытые живым тестированием, и фиксы:
- Ложный успех скачивания («скачано» за минуту, весов нет): репозитории с весами *.fp16.safetensors (например, Juggernaut XL v9) не ловились жёсткими allow_patterns. Фикс в core/model_downloader.py: чтение состава репозитория через list_repo_files + динамический выбор весов (предпочтение fp16), явная ошибка, если весов нет.
- Прогрессбар висел на 5%: парсер разбивал вывод только по переводам строки, а прогресс пишется через возврат каретки. Фикс: разбивка по обоим символам в обоих загрузчиках.
- Загрузка модели падала («no file named diffusion_pytorch_model.safetensors»): from_pretrained без variant не видит fp16-весов. Фикс в scripts/generate_diffusers.py: детекция варианта по фактическим файлам + передача variant="fp16".
- Диалог прыгал на вкладку Ollama после скачивания: пересоздание вкладок сбрасывало индекс. Фикс: сохранение и восстановление текущей вкладки.

Проверено вживую (Manjaro): полный цикл скачивания Джангернаута (веса, верификация, появление в селекторе моделей), скачивание qwen2.5:3b с живым прогрессбаром, загрузка модели в генерацию после фикса варианта.

Заметки:
- Дефолтная витрина (по 3 модели в секции) — решение Этапа 1; расширяется через data/shared/registry/available_models.json без правки кода.
- Первая загрузка модели с внешнего диска медленная (холодное чтение нескольких ГБ); повторные быстрее за счёт кэша ОС. Конвертация весов выполняется в памяти при каждой загрузке и на диск не сохраняется.
- Латентный баг (не фиксил): чувствительность _beautify_name к регистру; в Менеджере обходится сравнением по full_name.

Следующий шаг: консолидация — влить управление моделями (удаление/открытие папки) из diffusers_models_dialog.py в Менеджер, убрать дубль «управление модели» из настроек Diffusers.

## 2026-08-30 (сессия 2): Краш QThread — причина найдена и устранена ✅

**Цель:** устранить краш `QThread: Destroyed while thread '' is still running` при закрытии настроек.

**Точечная диагностика (по наблюдениям Корбена):**
- Цепочка исследования: `folder_dialog.py` → `chat_settings_widget.py` → `settings_dialog.py` → `update_settings_widget.py` → `core/updater.py` → `main_window.py`
- Гипотеза про обёртку FolderDialog не подтвердилась: `chat_settings_widget.py` использует `QFileDialog.getExistingDirectory` напрямую
- Ключевая зацепка: краш иногда не происходил при смене папки чатов — модальный `QFileDialog` запускает вложенный event loop, поток успевал завершиться

**Корневая причина:**
- `UpdateSettingsWidget.__init__` сразу вызывает `updater.check_for_updates()` → `_VersionCheckWorker(QThread)` с блокирующим `urlopen(timeout=10)`
- `SettingsDialog.closeEvent` → `updater.shutdown()` → `wait(3000)` ждёт лишь 3 секунды
- Закрытие настроек быстрее, чем `urlopen` отпустит поток → QThread уничтожается на ходу → краш

**Директива Корбена:** «Ни один вызванный пользователем виджет не должен ждать нисколько секунд, если пользователь решит его закрыть». Переделывать систему, а не латать.

**Решение — переделка `core/updater.py` на v2.1:**
- Проверка версий: `QNetworkAccessManager` вместо QThread (полностью асинхронно; `shutdown()`/`wait()` не нужны — Qt сам отменяет запросы при уничтожении объекта)
- Словарь `_pending_requests`: маппинг reply → тип запроса (version_check / changelog)
- `UpdateWorker(QThread)` для скачивания/установки архива сохранён (тяжёлая задача, стартует только по кнопке)

**Сопутствующие правки:**
- `ui/dialogs/settings/settings_dialog.py`: убран вызов `updater.shutdown()` из `closeEvent`
- `ui/main_window.py`: `Updater()` → `Updater(self)` (дерево родителей Qt)

**Результат:** тесты пройдены — быстрое закрытие настроек, закрытие после ожидания, закрытие после «Обзор» — краша нет.

**Следующий шаг:** тест нового модуля обновлений на релизе.

## 2026-08-30 (сессия 1) — Диагностика краша QThread (не устранён, откат)

**Цель сессии**: найти и устранить краш `QThread: Destroyed while thread '' is still running` при закрытии настроек.

**Что сделано**:
- Проанализирован лог краша
- Проверены файлы: `core/ollama_manager.py`, `core/ollama_model_info.py`, `ui/main_window.py`, `core/resource_manager.py`, `ui/tabs/diffusers_tab.py`, `ui/tabs/ollama_tab.py`, `core/updater.py`, `ui/dialogs/settings/settings_dialog.py`, `ui/dialogs/settings/update_settings_widget.py`, `core/ollama_client.py`, все виджеты настроек
- Найдены подозрительные места:
  - `core/updater.py`: `_VersionCheckWorker(QThread)` с блокирующим `urlopen(timeout=10)`, `shutdown()` ждёт только 3 секунды
  - `ui/dialogs/settings/paths_settings_widget.py`: `self._ollama_retry_timer = QTimer()` без parent
- Применены фиксы:
  - `core/updater.py`: добавлен `_stop_requested` флаг и метод `stop()` в `_VersionCheckWorker`, `shutdown()` вызывает `stop()` перед `wait()`
  - `ui/dialogs/settings/paths_settings_widget.py`: `QTimer()` заменён на `QTimer(self)`
- **Результат: краш не устранён**, оба файла откачены

**Выводы для следующей сессии**:
- Краш происходит сразу после `OllamaManager.start()` завершился, ещё до открытия настроек
- Значит проблема не в настройках, а в самом старте приложения
- Нужен другой подход: запустить с отладкой (`gdb`, `faulthandler`, или добавить `print()` в подозрительные места)
- Подозрительные места для проверки завтра:
  - `core/ollama_manager.py`: `_wait_ready()` создаёт `_wait_timer = QTimer()` без parent
  - `ui/main_window.py`: `Updater()` создаётся без parent, `QTimer.singleShot(3000, ...)` для проверки обновлений
  - `core/resource_monitor.py`: мониторинг RAM/CPU каждые 2 секунды (не проверен)
  - `ui/shared_bottom_bar.py`: индикаторы RAM/CPU (не проверен)

---


## Сессия 2026-08-29: Выбор папки чата без навигации

### Проблема
- Стандартный `QFileDialog` при выборе папки чата позволял входить внутрь двойным кликом
- Пользователь хотел **выбрать** папку, а не навигировать по ней
- Это ломало UX: клик по папке чата открывал её содержимое вместо выбора

### Решение
- **Обёртка `FolderDialog`** с двумя режимами:
  - `mode="navigate"` — стандартное поведение (для настройки путей, чекпоинтов)
  - `mode="select"` — вход в папку = выбор, диалог закрывается (для чатов)
- **Сигнал `directoryEntered`** — публичный API Qt, срабатывает при входе в папку
- **`QTimer.singleShot(0, dialog.accept)`** — отложенное закрытие (без него `accept()` внутри обработчика сигнала не срабатывал из-за реентерабельности)
- **`ShowDirsOnly`** — исправил фильтр типов файлов (стало "Папка" вместо "Все файлы")
- **Игнорирование стартовой директории** — защита от закрытия при открытии диалога

### Изменённые файлы
- **ui/dialogs/folder_dialog.py** (новый) — обёртка над QFileDialog с режимами
- **ui/settings_panel.py** — заменён вызов `QFileDialog.getExistingDirectory` на `FolderDialog.get_folder` с `mode="select"`

### Результат
- Клик по папке чата = выбор и закрытие диалога
- Двойной клик = тоже выбор (стандартное поведение при входе)
- Кнопка "Отмена" работает корректно
- Фильтр типов файлов показывает "Папка", а не "Все файлы"
- Решение переиспользуемо (можно применить к чекпоинтам в Diffusers при необходимости)

### Заметки
- Проверено на Manjaro (KDE Plasma)
- На Ubuntu (GNOME) с нативным GTK-диалогом `DontUseNativeDialog` должен принудительно использовать Qt-диалог — проверим после релиза

---


## Сессия 2026-08-28: Счётчик контекста и загрузка файлов с карточкой

### Что сделано

#### Этап A: Счётчик контекста
- **Прогрессбар в нижней панели** теперь показывает токены (использовано / лимит), а не таймаут
- **Подкраска через палитру**: зелёный < 70%, оранжевый 70-90%, красный > 90%
- **Обновление при**: смене модели, изменении промпта, добавлении сообщений в чат
- **Кэш лимитов моделей**: модуль **ollama_model_info.py** запрашивает /api/show с TTL 5 мин, чтобы не долбить сервер при каждом чихе

#### Этап B: Загрузка файлов с карточкой
- **Карточка в чате**: 📄 имя · размер · ✕
- **Клик по имени** → системный xdg-open (или модальное окно с ошибкой, если файл удалён)
- **Клик по ✕** → удаление вложения из промпта
- **При отправке** вложения впекаются в запрос к модели (через **markdown_parser.render_file_attachment**)
- **Блокировка кликов** во время генерации (защита от краша Wayland при модальных окнах)

### Изменённые файлы
- **core/ollama_model_info.py** (новый) — кэш лимитов контекста моделей
- **core/context_tracker.py** (новый) — трекер токенов с эмиссией прогресса
- **core/file_reader.py** (новый) — чтение и валидация файлов для вложений
- **core/markdown_parser.py** — добавлен метод render_file_attachment
- **ui/chat_widget.py** — сигналы open_file_requested и remove_file_requested
- **ui/shared_bottom_bar.py** — прогрессбар токенов вместо таймаута
- **ui/main_window.py** — интеграция счётчика контекста
- **ui/tabs/ollama_tab.py** — методы _on_attach_file, _on_open_file, _on_remove_file

---



## Сессия 2026-08-26: Чистка мёртвого кода ветвления + концептуальное решение навигации

### Что сделано

#### Масштабная чистка мёртвого кода (−334 строки)

Проведена полная зачистка наследия отброшенных итераций дизайна ветвления:

1. **Удалены паспорта сообщений (`core/chat_manager.py`)**:
   - Убран импорт `uuid` и все выдачи `"id": str(uuid.uuid4())` в `add_user_message`, `add_assistant_message`, `load_messages`.
   - Сообщения теперь идентифицируются только по `user_msg_index` (стабильный индекс, как ID в БД).

2. **Удалена паспортная навигация (`core/chat_versions.py`)**:
   - Метод `find_alternatives` (38 строк) — полностью удалён.
   - Это была старая логика сравнения паспортов префиксов между чатами, заменена на чтение вариантов прямо из сообщения.

3. **Удалён кэш веток (`core/branch_cache.py`)**:
   - Файл удалён целиком (63 строки).
   - Больше не нужен после отказа от модели "ветки как файлы".

4. **Чистка `ui/tabs/ollama_tab.py` (~150 строк)**:
   - Удалены мёртвые атрибуты: `_current_branches`, `_is_branching`, `_branch_from_index`, `branch_cache`, `_pending_branch_tail`.
   - Удалены мёртвые методы: `_on_switch_branch_requested`, `_switch_to_branch`, `_branch_preview`, `_read_disk_branch`, `_merged_branches`, `_branches_count_for`.
   - Удалён мёртвый блок `if self._is_branching` в `_on_export_chat`.
   - Вызов `_branches_count_for` в `handle_prompt` заменён на `self._chat_number` (корректная семантика для подсветки кнопок навигации).

5. **Упрощён `core/chat_exporter.py`**:
   - Убрана вся логика создания веток: параметры `is_branch`, `parent_user_msg_index`, архивация `main.json` в `branch_*.json`, поле `branches` в JSON.
   - Докстринг обновлён (убрано упоминание ветвления).
   - Метод `export_chat` теперь просто сохраняет `main.json` и `main.txt`.

6. **Чистка `ui/chat_widget.py`**:
   - Удалён мёртвый сигнал `switch_branch_requested` (не эмитился нигде).
   - Удалён диспетчер якоря `#branches:` (не генерировался парсером).

7. **UX: устранение тёзок в интерфейсе (`core/markdown_parser.py`)**:
   - Кнопка ветвления под сообщением переименована: «✏ Изменить» → «✏ Правка».
   - Режим в настройках оставлен как «Изменить» (тройка "Новый / Продолжить / Изменить" логична).

8. **Фикс ошибочной логики режима «Изменить» (`ui/tabs/ollama_tab.py`)**:
   - Режим «Изменить» в настройках больше не создаёт ветки при сохранении (это было следствием путаницы с одноимённой кнопкой ветвления).
   - Теперь просто сохраняет чат, как режим «Продолжить».

#### Концептуальное решение: навигация по иерархии веток

Корбен нашёл поведение, которое выглядело как баг: при возврате к верхним сообщениям и ветвлении оттуда, нижние ветки не видят новые варианты.

**Анализ:** при ветвлении от узла `N` синхронизация обновляет варианты только в этом узле, а не в префиксе. Поэтому чаты, созданные позже в других ветках, не попадают в варианты префикса старых чатов.

**Решение:** это **не баг, а корректное поведение**. Принцип: «ветки живут в своей иерархии». Если правка сделана внутри одной ветки, она принадлежит только ей и не должна «протекать» обратно в другие ветки. Навигация показывает только то, что релевантно в текущей ветке. Чтобы добраться до любой ветки, пользователь проходит обратную последовательность выбора.

Это философски красивое и простое решение, соответствующее принципу «простота = надёжность».

### Коммиты

- `7915a83` — refactor(chat): чистка мёртвого кода ветвления + переименование кнопки Правка

---

## Сессия 2026-08-24: Фикс автозапуска Ollama + восстановление UI + начало ветвления диалога

### Что сделано

#### Фиксы и улучшения
1. **Асинхронное ожидание запуска Ollama (`core/ollama_manager.py`)**:
   - Метод `_wait_ready` переписан с синхронного `time.sleep` на асинхронный `QTimer`. UI больше не зависает при медленном старте.

2. **Автозапуск Ollama при переподключении диска (`ui/main_window.py`)**:
   - В `_show_settings_dialog` добавлена проверка: если пути не изменились, но бинарник стал доступен — сервер запускается автоматически.

3. **Умная проверка в виджете настроек (`ui/dialogs/settings/`)**:
   - `PathsSettingsWidget` теперь передаёт `OllamaManager` и вызывает `start()` перед проверкой HTTP-подключения. Кнопка 🔄 запускает сервер, а не только пингует.

4. **Восстановление UI при загрузке чата (`ui/chat_widget.py`, `ui/tabs/ollama_tab.py`)**:
   - Убрано ограничивающее условие в `append_assistant_message` — блок статистики/копирования теперь рендерится всегда, даже если `stats` пустой.
   - В `ollama_tab.py` метод `on_finished` теперь передаёт `self.last_stats` в `chat_manager.add_assistant_message`, чтобы статистика сохранялась в JSON и воспроизводилась при загрузке.

5. **Логика сохранения чата по режимам (`core/chat_exporter.py`, `ui/tabs/ollama_tab.py`)**:
   - `ChatExporter.export_chat()` получил параметр `target_base_path` для перезаписи существующего файла.
   - В `OllamaTab` запоминание пути и заголовка загруженного чата (`_current_chat_base_path`, `_current_chat_title`).
   - **Режим "Продолжить"**: 💾 сохраняет в тот же файл молча, без диалога.
   - **Режим "Изменить"**: 💾 показывает диалог выбора (перезаписать / сохранить как новый / отмена).
   - **Режим "Новый"**: 💾 работает как раньше (диалог ввода имени или автогенерация).
   - Убрана разморозка UI после сохранения — пользователь может продолжить писать в том же режиме.

#### Архитектура ветвления диалога (в процессе реализации)

**Концептуальные решения:**
- **Файловая архитектура**: Каждый чат = папка в `data/ollama/chats/{название_чата}/`
  - `main.json` — последняя активная последовательность + словарь `branches`
  - `branch_YYYY-MM-DD_HH-MM.json` — ветки (техническое имя)
  - Пользователь выбирает папку, а не файл
  - Прозрачность: все ветки видны в файловом менеджере

- **Стабильные индексы**: Каждое сообщение пользователя получает `user_msg_index` (как ID в БД)
  - При обрезке индексы не пересчитываются
  - Ветки привязываются к конкретному `user_msg_index`
  - Обратная совместимость: старые чаты автоматически получают индексы при загрузке

- **Кнопки под сообщениями пользователя** (по аналогии со статистикой под ответами модели):
  - **✂️ Обрезать** — удаляет всё после этого сообщения
  - **🌿 Ответвиться** — создаёт ветку от этого сообщения
  - **📂 N** — счётчик веток от этого узла (клик открывает список альтернатив)

**Реализовано:**
1. **Структура JSON (`core/chat_manager.py`)**:
   - Добавлен `_user_msg_counter` и автоматическая нумерация `user_msg_index`
   - Обратная совместимость: при загрузке старых чатов индексы назначаются автоматически

2. **Экспорт в папки (`core/chat_exporter.py`)**:
   - Метод `export_chat()` принимает `chat_folder_name`, `is_branch`, `parent_user_msg_index`
   - Создаёт папку с санитизированным именем чата
   - При `is_branch=True` создаёт `branch_*.json` и обновляет `main.json` (добавляет ветку в словарь `branches`)

3. **UI кнопок (`core/markdown_parser.py`, `ui/chat_widget.py`)**:
   - `render_user_message()` теперь принимает `user_msg_index` и `branches_count`
   - Рендерит кнопки ✂️ / 🌿 / 📂 под сообщением пользователя
   - Добавлены сигналы `trim_requested`, `branch_requested`, `switch_branch_requested`

4. **Логика обрезки и ветвления (`ui/tabs/ollama_tab.py`)**:
   - Методы `_on_trim_requested()`, `_on_branch_requested()`, `_on_switch_branch_requested()`
   - Флаги `_is_branching` и `_branch_from_index` для отслеживания режима ветвления
   - При сохранении в режиме ветвления создаётся `branch_*.json` и обновляется `main.json`

**Не завершено:**
- Кнопка 📂 не появляется после создания ветки (проблема с обновлением `_current_branches` в UI)
- Диалог выбора ветки при клике на 📂 N
- Загрузка папок вместо файлов (диалог выбора)
- Переключение между ветками

### Известные проблемы (TODO)
- **Баг**: После сохранения чата кнопка "+ Новый чат" показывает диалог "Текущий чат не сохранён", хотя чат уже сохранён. Нужно скорректировать проверку в `_on_new_chat_clicked` с учётом `_current_chat_base_path`.
- **Баг**: Если прервать генерацию (кнопка "Стоп"), неполные ответы вызывают ошибку при нажатии на кнопку "Отменить" (↶). Последняя пара сообщений не удаляется, блокируя возможность отмены более ранних сообщений. Требуется синхронизация состояния `chat_manager` и `chat_widget` при прерывании.
- **Баг**: Кнопка 📂 N не появляется после создания ветки. Проблема в том, что `_current_branches` не обновляется в UI после сохранения. Нужно перерисовывать чат с обновлёнными кнопками.

### Коммиты
- `1161811` — fix(ollama): асинхронный старт сервера, автозапуск при переподключении диска и умная проверка в настройках
- `7902814` — feat(chat): логика сохранения по режимам (Продолжить/Изменить/Новый) + фикс разморозки UI
- `635b98d` — docs: обновление WORKLOG (логика сохранения по режимам + TODO)
1. **Асинхронное ожидание запуска (`core/ollama_manager.py`)**:
   - Метод `_wait_ready` переписан с синхронного `time.sleep` на асинхронный `QTimer`. Это полностью устранило зависание UI при медленном старте Ollama (например, с внешнего диска).
2. **Автозапуск при переподключении диска (`ui/main_window.py`)**:
   - В `_show_settings_dialog` добавлена проверка: если строки путей не изменились, но бинарник Ollama стал доступен (диск переподключён), сервер автоматически запускается.
3. **Умная проверка в виджете настроек (`ui/dialogs/settings/`)**:
   - В `PathsSettingsWidget` перед проверкой HTTP-подключения теперь вызывается `ollama_manager.start()`, если сервер не запущен. Кнопка 🔄 теперь не просто пингует, а гарантирует запуск сервиса перед проверкой статуса.

### Коммиты
- *(текущий)* — fix(ollama): асинхронный старт сервера, автозапуск при переподключении диска и умная проверка в настройках

---


## Сессия 2026-08-22: Сохранение/загрузка чатов + режимы работы + фикс бага очистки

### Что сделано
1. **Модуль экспорта чатов (`core/chat_exporter.py`)**:
   - Сохранение в JSON (для машины: точное воспроизведение, настройки, stats) и TXT (для человека: markdown с разделителями).
   - Генерация имён файлов: дата + санитизированный заголовок (с fallback на timestamp).
   - Автогенерация заголовка через LLM (опционально, через настройки).

2. **Режимы работы чата (`ui/settings_panel.py`, `ui/tabs/ollama_tab.py`)**:
   - Добавлены радиокнопки: "Новый", "Продолжить", "Изменить".
   - Логика блокировки UI: в режиме "Продолжить" настройки заблокированы (чтобы не ломать контекст), в "Изменить" — активны.
   - Загрузка чата из JSON через кнопку `📂` с применением оригинальных настроек.

3. **Исправление критического бага**:
   - Кнопка "+ Новый чат" теперь корректно ждёт завершения асинхронного сохранения перед очисткой истории (добавлен флаг `_pending_clear_after_save`). Раньше чат очищался мгновенно, и сохранялся пустым.

4. **Документация**:
   - Добавлен `chat_exporter.py` в `docs/STRUCTURE.md`.

### Коммиты
- *(текущий)* — feat(chat): сохранение/загрузка чатов (JSON/TXT), режимы работы, фикс бага очистки перед сохранением

---

## Сессия 2026-08-20: Интерфейс сохранения чатов (Этап 1) + UX-фиксы
*(См. предыдущие записи: панель управления чатом, plain text в промпте, фикс фокуса при копировании, ожидание TIME_WAIT для Ollama)*

---

## Сессия 2026-08-18: Реорганизация LLM-чата
*(Append-only рендеринг, live-статусбар, таблицы GFM, вложенные списки)*

---

## 📜 Архивные решения (10–16 августа 2026)
*(Модуль обновлений v2.0, миграция структуры data/, автоустановка Python 3.12, model_validator.py)*

---
> **Примечание**: Этот файл хранит только ключевые архитектурные решения и контекст последних сações. Для деталей см. историю коммитов Git.

---

## Сессия 2026-08-23: Теоретическое планирование архитектуры

### Обсуждаемые темы

1. **Архитектура сохранения чатов**:
   - Утверждён гибридный формат: JSON (для машины/RAG, точное воспроизведение) + TXT (для человека, markdown с разделителями).
   - Две галочки в настройках (вкладка "Чат"): можно отключить любой формат.
   - Автосохранение после каждого сообщения + кнопка "💾 Сохранить" для экспорта.

2. **Управление контекстом LLM**:
   - Обсуждён `ContextTracker` для приближённого подсчёта токенов (символы × коэффициент).
   - Индикатор в статусбаре: "45K / 128K токенов (35%)" с цветовой индикацией.
   - Предупреждения при переполнении (>90% контекста).

3. **Перевод промптов для Diffusers**:
   - Выявлена возможность использования сверхлёгких LLM (`qwen2.5:1.5b`) для мгновенного перевода визуальных промптов.
   - Контекст мизерный (физика, материалы, освещение) — маленькие модели справляются отлично.
   - Кнопка "🌐 Перевести промпт" в DiffusersTab — будущая фича.

4. **Ollama Image Generation (анализ)**:
   - **Идея**: Добавить "Express-режим" генерации изображений через нативный API Ollama (модели `x/flux2-klein`, `x/z-image-turbo`) как лёгкую альтернативу тяжёлому стеку Diffusers.
   - **Статус**: ⏸️ **Отложено (Backlog)**.
   - **Причина**: На текущий момент (январь 2026) экспериментальная поддержка генерации изображений в Ollama доступна **только на macOS**. Linux-поддержка ожидается в будущих релизах.
   - **Архитектурные заметки на будущее**:
     - Не заменять текущий `DiffusersTab`, а добавить переключатель режимов (Express/Pro).
     - Express-режим будет использовать `ollama_client.py`, не требует `torch`/`diffusers`, но **не поддерживает** чекпоинты, resume, ControlNet и тонкую настройку scheduler (black box).
     - Потребление RAM: ~4-6 ГБ (для 4B моделей), что хорошо для слабых машин, но требует контроля в `resource_manager.py`.

### Архитектурные решения

- **Гибридный формат хранения чатов**: JSON (структура, метаданные, для RAG) + TXT (человекочитаемый markdown, для архива). Затраты пространства мизерные (~230 KB на чат из 100 сообщений).
- **Express vs Pro режимы**: Ollama Image Gen — это "мыльница" (простота, экономия RAM), Diffusers — "зеркальная камера" (контроль, чекпоинты, resume). Они дополняют, а не заменяют друг друга.
- **Контекстный трекер**: Приближённый подсчёт токенов (символы ÷ 4 для английского, ÷ 2 для русского) достаточно для UI. Точный подсчёт через токенизатор — overkill.

### Отложенные задачи (Backlog)

- **Ollama Image Gen (Express-режим)**: Интеграция нативной генерации изображений через Ollama. *Статус: отложено до официального выхода Linux-поддержки в Ollama.*
- **Быстрый перевод промптов**: Использование лёгкой LLM (1.5B) для мгновенного перевода русских визуальных промптов в оптимизированный английский формат для CLIP.

### Коммиты

- *(текущий)* — docs: теоретическое планирование архитектуры (сохранение чатов, контекст, Ollama Image Gen анализ)


---

## Сессия 2026-08-23 (дополнение): Анализ OllamaDiffuser

### Найденный проект
- **OllamaDiffuser** (LocalKinAI/ollamadiffuser) — сторонняя Python-библиотека, имитирующая UX Ollama для генерации изображений
- **Сайт**: https://www.ollamadiffuser.com/
- **GitHub**: https://github.com/LocalKinAI/ollamadiffuser
- **Версия**: v2.0.17 (май 2026)
- **Установка**: `pip install "ollamadiffuser[gguf]"` (для Linux с поддержкой квантованных моделей)

### Ключевые особенности
1. **Реестр 60+ моделей** (FLUX.1, FLUX.2 Klein, SANA, SD 3.5, Z-Image и др.) с метаданными (VRAM, шаги, лицензия)
2. **Поддержка GGUF** — квантование для запуска на слабом железе (3-8 GB VRAM вместо 20+ GB)
3. **MLX Backend** для Apple Silicon (2-3× быстрее PyTorch+MPS)
4. **Стратегия Lazy Loading** — препроцессоры грузятся только при первом запросе
5. **Python API**: `from ollamadiffuser.core.models.manager import model_manager`
6. **MCP сервер** — интеграция с AI-ассистентами (Claude Code, Cursor)

### Архитектурное решение
- **Express-режим** (будущая фича LocalAILite) будет использовать `ollamadiffuser` как альтернативный движок
- **Pro-режим** (текущий `scripts/generate_diffusers.py`) остаётся для задач с чекпоинтами, resume и полным контролем
- **Переключатель режимов** будет добавлен в `ui/tabs/diffusers_settings_panel.py` (радиокнопки Pro/Express)
- **Расширение `core/models_registry.py`** — добавление флага `engine: "express"` для моделей из реестра OllamaDiffuser

### Важные уточнения
- Это **не** экспериментальная фича официального Ollama (которая пока только на macOS)
- Это **сторонняя библиотека**, которая работает на Linux/Windows через GGUF и оптимизированные diffusers
- Интеграция возможна **прямо сейчас**, не требует ожидания релизов от разработчиков Ollama

### Влияние на текущие приоритеты
- **Не меняет** последовательность задач из HANDOFF.md (восстановление UI, ветвление, загрузка файлов)
- Относится к **Этапу 3 (Оптимизация и расширение)** — после закрытия приоритетных задач v1.6.0
- Зафиксировано как утверждённая стратегия для будущего спринта по Express-режиму

